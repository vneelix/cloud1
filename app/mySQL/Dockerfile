FROM	mysql

ARG		TCP_PORT
ENV		MYSQL_TCP_PORT=${TCP_PORT}

ARG		ROOT_PASS=root
ENV		MYSQL_ROOT_PASSWORD=${ROOT_PASS}

ARG		DATABASE=storage
ENV		MYSQL_DATABASE=${DATABASE}

ARG		USER=user
ENV		MYSQL_USER=${USER}

ARG		PASSWORD=user
ENV		MYSQL_PASSWORD=${PASSWORD}
